Blockly.JavaScript['renk_1'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var colour_color = block.getFieldValue('color');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['renk_2'] = function(block) {
  var dropdown_sensor_id = block.getFieldValue('sensor_id');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['renk_3'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var dropdown_operator = block.getFieldValue('operator');
  var number_percent = block.getFieldValue('percent');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['renk_4'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['tof_1'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var dropdown_name = block.getFieldValue('NAME');
  var number_value = block.getFieldValue('value');
  var dropdown_unit = block.getFieldValue('unit');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['tof_2'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var dropdown_unit = block.getFieldValue('unit');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['sound_1'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['sound_2'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var dropdown_option = block.getFieldValue('option');
  var number_value = block.getFieldValue('value');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['sound_3'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  var dropdown_option = block.getFieldValue('option');
  var number_name = block.getFieldValue('NAME');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};

Blockly.JavaScript['sound_4'] = function(block) {
  var dropdown_sensorid = block.getFieldValue('sensorID');
  // TODO: Assemble JavaScript into code variable.
  var code = '...';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.JavaScript.ORDER_NONE];
};